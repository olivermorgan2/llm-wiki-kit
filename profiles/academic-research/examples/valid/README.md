# academic-research valid fixtures

One valid page per added/tightened type (`source`, `claim`, `method`,
`question`, `synthesis`). Validated together as one bundle under the
academic-research profile they yield **zero** findings (cross-links resolve, and
every page carries the core recommended fields so no incidental
`core-recommended-missing` fires). Backs acceptance criteria 5, 7, 8, 9 for the
profile. Asserted by `TestAcademicValidExamplesHaveNoFindings`.
